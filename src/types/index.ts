export interface Contributor {
    id: string
    name: string
    image: string
    role: string
}

export interface Project {
    id: string
    title: string
    description: string
    logo: string
    license: string
    repoUrl: string
} 